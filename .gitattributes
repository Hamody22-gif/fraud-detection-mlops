# Normalize all text files to LF inside the repository
* text=auto eol=lf

# Windows scripts must keep CRLF or they break
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Never touch these — treat as binary
*.csv binary
*.parquet binary
*.pkl binary
*.joblib binary
